# O app conecta a um PostgreSQL remoto (definido no .env) — não há banco local.
services:
  app:
    build: .
    container_name: sinan-analytics
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
